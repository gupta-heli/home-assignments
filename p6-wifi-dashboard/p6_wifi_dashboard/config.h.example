#ifndef CONFIG_H
#define CONFIG_H

// Copy this file to config.h and update with your actual Wi-Fi credentials.
// config.h is gitignored to avoid publishing sensitive credentials.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

#endif // CONFIG_H
