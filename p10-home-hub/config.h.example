#ifndef CONFIG_H
#define CONFIG_H

// Copy this file to config.h and fill in your own values.
// config.h must be added to .gitignore - never commit real Wi-Fi passwords.

#define WIFI_SSID     "your_wifi_name"
#define WIFI_PASSWORD "your_wifi_password"
#define MQTT_USERNAME "yourname"   // used to build topic iitjammu/<yourname>/home

#endif // CONFIG_H
